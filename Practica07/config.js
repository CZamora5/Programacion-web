/*
  En este archivo hacemos un mapeo de nombres de variables a strings, lo cual nos ayudará a evitar typos
  y servirá para hacer cambios en los nombres de los eventos, en caso de ser necesario, en un único lugar
*/

module.exports = {
    events: {
        GREET: 'greet',
        JUMP: 'jump'
    }
}